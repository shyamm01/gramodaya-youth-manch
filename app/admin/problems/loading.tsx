import React from 'react';
import { GrievancesSkeleton } from '@/src/components/admin/grievances/GrievancesSkeleton';

/** Route-level placeholder — see the component for why it is shaped this way. */
export default function AdminProblemsLoading() {
  return <GrievancesSkeleton />;
}
