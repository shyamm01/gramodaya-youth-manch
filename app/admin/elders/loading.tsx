import React from 'react';
import { EldersSkeleton } from '@/src/components/admin/elders/EldersSkeleton';

/** Route-level placeholder — see the component for why it is shaped this way. */
export default function AdminEldersLoading() {
  return <EldersSkeleton />;
}
