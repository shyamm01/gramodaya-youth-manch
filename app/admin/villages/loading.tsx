import React from 'react';
import { VillagesSkeleton } from '@/src/components/admin/villages/VillagesSkeleton';

/** Route-level placeholder — see the component for why it is shaped this way. */
export default function AdminVillagesLoading() {
  return <VillagesSkeleton />;
}
