import React from 'react';
import { SocialWorkSkeleton } from '@/src/components/admin/social-work/SocialWorkSkeleton';

/** Route-level placeholder — see the component for why it is shaped this way. */
export default function AdminSocialWorkLoading() {
  return <SocialWorkSkeleton />;
}
